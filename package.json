{
  "name": "webgpu-kitten-tts",
  "version": "0.1.0",
  "description": "Run Kitten TTS (80M) locally in the browser via WebGPU. Zero dependencies.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "phonemizer": "^1.2.1"
  }
}
